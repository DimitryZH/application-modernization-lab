# Docker Compose to .NET Aspire

The .NET Aspire work will be tracked separately after the Docker Compose baseline is validated.

This directory intentionally contains no Aspire code, AppHost, ServiceDefaults, design, or migration files.
